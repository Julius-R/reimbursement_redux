import withSession from "../lib/Session";
import {
	Container,
	useColorModeValue,
	FormControl,
	FormLabel,
	Input,
	Button,
	Grid,
	GridItem,
	Spinner,
	Textarea,
	Heading
} from "@chakra-ui/react";

export default function Admin({ user }) {
	return <>Admin</>;
}
